// Class used to store the names of the pages in the app
class PageNames {
  static const _pageNames = [
    "Buildings",
    "Map",
    "360 View",
    "Settings",
  ];

  // Gets the names of the app's pages
  static List<String> getNames() {
    return _pageNames;
  }
}
