import 'dart:ui';

// Class used to store commonly used widget styling
class AppStyleProperties {
  // Colors
  static const uwoshYellow = Color.fromARGB(255, 255, 204, 0);

  // Font Sizes
  static const fontSize18 = 18.0;

  // Padding
  static const padding8 = 8.0;
  static const padding16 = 16.0;
  static const padding20 = 20.0;

  // Durations
  static const duration500ms = 500;

  // Width/Heigh factors
  static const widthFactor = 0.7;

  // Radius
  static const radius18 = 18.0;

  // Icon Sizes
  static const iconSize = 28.0;
}
